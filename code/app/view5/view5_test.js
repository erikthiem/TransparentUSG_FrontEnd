'use strict';

describe('myApp.view5 module', function() {

  beforeEach(module('myApp.view5'));

  describe('view5 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View5Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
